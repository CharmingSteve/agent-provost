services:
  alpaca-mcp:
    image: ${ALPACA_IMAGE}@${ALPACA_IMAGE_TAG}
    pull_policy: if_not_present
    container_name: alpaca-mcp
    volumes:
      - ./entrypoint.sh:/entrypoint.sh:ro
      - ${PROVOST_SECRETS_DIR:-./.secrets}:/run/secrets:ro
    entrypoint: ["/bin/sh", "/entrypoint.sh"]
    environment:
      TRADE_API_URL: "http://agent-provost:8081/trading"
      DATA_API_URL: "http://agent-provost:8081/data"
      BROKER_API_URL: "http://agent-provost:8081/broker"
      ALPACA_API_KEY: ${ALPACA_API_KEY:-}
      ALPACA_SECRET_KEY: ${ALPACA_SECRET_KEY:-}
    expose:
      - "8088"
    networks:
      - mcp_internal
    security_opt:
      - no-new-privileges:true
    user: "10001:10001"
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
    pids_limit: 256
    restart: unless-stopped

  fluent-bit:
    image: ${FLUENT_BIT_IMAGE}
    pull_policy: if_not_present
    container_name: fluent-bit
    command: ["/fluent-bit/bin/fluent-bit", "-c", "/fluent-bit/etc/fluent-bit.conf"]
    volumes:
      - ./fluent-bit:/fluent-bit/etc:ro
      - provost_run:/var/run/provost
      - ./logs/fluent-bit-storage:/var/log/fluent-bit/storage
    environment:
      AWS_REGION: ${AWS_REGION:-us-east-1}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_SESSION_TOKEN: ${AWS_SESSION_TOKEN:-}
      S3_BUCKET: ${S3_BUCKET:-agent-provost-local}
      INSTANCE_ID: ${INSTANCE_ID:-local-dev}
    healthcheck:
      test: ["CMD", "/fluent-bit/bin/fluent-bit", "--version"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - mcp_internal
      - proxy_egress
    security_opt:
      - no-new-privileges:true
    user: "65532:65532"
    read_only: true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
    pids_limit: 256
    restart: unless-stopped
  agent-provost:
    # Digest is sourced from .env.versions via: docker compose --env-file .env.versions ...
    image: ${OPENRESTY_IMAGE}
    pull_policy: if_not_present
    container_name: agent-provost
    depends_on:
      alpaca-mcp:
        condition: service_started
      fluent-bit:
        condition: service_healthy
    ports:
      - "8088:8000"
    expose:
      - "8081"
    volumes:
      - ./default.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
      - ./lua:/etc/nginx/lua:ro
      - ./rules.json:/etc/rules.json:ro
      - ${PROVOST_SECRETS_DIR:-./.secrets}:/run/secrets:ro
      - provost_run:/var/run/provost
    networks:
      - mcp_internal
      - proxy_egress
    security_opt:
      - no-new-privileges:true
    user: "65532:65532"
    read_only: true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
    pids_limit: 256
    restart: unless-stopped

networks:
  mcp_internal:
    internal: true
  proxy_egress:

volumes:
  provost_run:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
      o: uid=0,gid=0,mode=0777
